"""Top-level backend package for EcoLang project."""

# This file makes `backend` a Python package so tests and imports like
# `from backend.ecolang.interpreter import Interpreter` work when running pytest
